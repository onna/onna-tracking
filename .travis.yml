language: node_js
node_js:
- 8.7.0
cache:
  directories:
    - node_modules
dist: trusty
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
before_install:
  - wget "http://chromedriver.storage.googleapis.com/2.24/chromedriver_linux64.zip"
  - unzip chromedriver_linux64.zip
  - sudo mv chromedriver /usr/local/bin
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - npm install -g yarn @angular/cli
  - npm install @team-griffin/install-self-peers
install:
  - yarn install
  - ./node_modules/.bin/install-self-peers
script:
  - npm run test
notifications:
  email:
    - ivan@onna.com
